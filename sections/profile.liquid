{% comment %} Display the settings {% endcomment %}
<div> 
  <div class="profile">
    <img src="{{ section.settings.user_img | img_url: '300x300' }}" alt="">
    
  <h3>
    {{ section.settings.user_name }}
  </h3>

  <p>
    {{section.settings.user_bio}}
  </p>


  </div>
</div>


{% comment %}  {% endcomment %}
{% schema %}
  {
    "name": "Profile",

    "settings": [
 
      
      {
        "type": "image_picker",
        "id": "user_img",
        "label": "User Photo"

      },
       {
        "type": "text",
        "id": "user_name",
        "label": "User name"
      },
         {
        "type": "richtext",
        "id": "user_bio",
        "label": "User bio"
      }


    ],

    "presets": [
      {
        "category": "Profile",
        "name": "User Profile  "  
       
      }
    ]
  }
{% endschema %}

{% comment %}  
Preset would show a default value for your section 

Settings: type: type of data, ID, and label. 
To display images you need to apply a filter 


{% endcomment%}